doctype html
html
  head
    title Post a Message
    link(rel='stylesheet', href='../styles/main.css')
  body
    h1 Error
    - for (var i = errors.length - 1; i >= 0; --i) {
      p= errors[i].msg
    - }
